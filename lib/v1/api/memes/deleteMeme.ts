// import axios, { AxiosResponse } from 'axios';

// import { Meme } from 'v1/types';

/**
 * Removes a meme by given meme index.
 * @param memeIndex number
 * @returns string
 */
export const deleteMeme = async (
	{ memeIndex }: { memeIndex: string },
	BASE_URL: string,
): Promise<string> => {
	return 'WIP';
};
