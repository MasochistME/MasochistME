import { WithId } from 'mongodb';

/**
 * This is a type of a single object within the collection "permissions".
 * A single object describes a single permission.
 */
export type Permission = WithId<{
	// TODO
}>;
