// import Discord from "discord.js";

// export const userHasRoleId = (
//   member: Discord.GuildMember | undefined,
//   roleId: string,
// ): boolean => !!member && member.roles.cache.some(role => role.id === roleId);

// export const userHasRoleName = (
//   member: Discord.GuildMember | undefined,
//   roleName: string,
// ): boolean =>
//   !!member && member.roles.cache.some(role => role.name === roleName);

export {};
